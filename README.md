# Laboratorio de Ansible - ITLA
Proyecto de automatización usando Ansible y Docker.

## Requisitos
- Docker y Docker Compose
- Ansible (vía WSL)

## Cómo ejecutar
1. Subir los contenedores: `docker-compose up -d`
2. Ejecutar el playbook: `ansible-playbook main.yml`
